# WebSocket bridge URL (defaults to ws://localhost:9090 if not set)
VITE_WS_URL=ws://localhost:9090

# Optional Twilio-compatible webhook for T=TODO ghost SMS broadcast payloads
VITE_TWILIO_SMS_WEBHOOK_URL=
